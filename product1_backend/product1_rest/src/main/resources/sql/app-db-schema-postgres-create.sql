CREATE TABLE CUSTOMER (ID  SERIAL NOT NULL, FIRSTNAME VARCHAR(255) NOT NULL, LASTMODIFIED TIMESTAMP NOT NULL, LASTNAME VARCHAR(255) NOT NULL, STATUS VARCHAR(255) NOT NULL, VERSION BIGINT, PRIMARY KEY (ID))
